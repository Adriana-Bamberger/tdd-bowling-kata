export { scoreFrame }

// const frames = [
//   [2, 0],

function scoreFrame(frames) {
  //let sum = 0
  //for (let i = 0; i < frames.length; i++) {
  const sum = frames[0] + frames[1]
  //}
  return sum
}
// scoreFrame(frames)
